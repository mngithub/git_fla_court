[Android Browser]
1. CSS แสดงผลไม่เหมือนกับ PC บาง CSS เช่น line-height ไม่เหมือนกัน ทำให้ความสูงของแต่ละแถวไม่เท่ากับ PC

[IE, CM]
1. อ่าน CSS @font-face ไม่ได้ทำให้ Font เพี้ยน ต้องทำการ install font ลงไปที่เครื่อง และเอา CSS @font-face ออก